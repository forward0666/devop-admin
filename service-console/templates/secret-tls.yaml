{{- if .Values.ingress.tls.enabled }}
# TLS secret is managed externally (wildcard-nip-tls)
# Copy from devops-admin namespace if needed:
# kubectl get secret wildcard-nip-tls -n devops-admin -o yaml | sed 's/namespace: devops-admin/namespace: {{ .Release.Namespace }}/' | kubectl apply -f -
{{- end }}
