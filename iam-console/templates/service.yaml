apiVersion: v1
kind: Service
metadata:
  name: {{ include "iam-console.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ include "iam-console.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "iam-console.name" . }}
